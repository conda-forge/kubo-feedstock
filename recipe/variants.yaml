c_stdlib:
  - if: linux
    then: sysroot
  - if: osx
    then: macosx_deployment_target
  - if: win
    then: vs

c_stdlib_version:
  - if: osx and x86_64
    then: 10.14

cgo_compiler:
  - go-cgo

cgo_compiler_version:
  - 1.24
